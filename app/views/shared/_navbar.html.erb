<nav class="navbar navbar-default">
  <div class="container-fluid">

    <div class="navbar-header">
      <a class="navbar-brand" href="#">
        <%= image_tag("logo.svg") %>
      </a>
      <a class="navbar-brand-name" href="#">
        <h4>Docu App</h4>
      </a>
    </div>

      <div class="navbar-authentication">
        <% if user_signed_in? %>
        <h4>
          <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "navbar-brand-name" %>
        </h4>
        <% else %>
        <h4>
          <%= link_to "Login", new_user_session_path, class: "navbar-brand-name" %>
        </h4>
        <% end %>
      </div>

  </div>
</nav>
